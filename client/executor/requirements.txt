websockets>=12.0
pyautogui>=0.9.54
mss>=9.0.1
pillow>=10.0.0
pygetwindow>=0.0.9
# Pulled in via `shared.protocol` (wire types) and `shared.config` (env reader).
pydantic>=2.0
# Floating-robot UI deps (client/scripts/voodo_assistant.py)
PyQt5>=5.15.0
websocket-client>=1.7.0
python-dotenv>=1.0.0
# Mouse-guide spotlight (client/scripts/mouse_guide.py, invoked by highlight_at)
pynput>=1.7.6
